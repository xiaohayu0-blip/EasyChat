spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:easy_chat_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  data:
    redis:
      repositories:
        enabled: false
  flyway:
    enabled: false
